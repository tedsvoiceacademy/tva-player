{
  "name": "@tva/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "The Windows app itself: window, files, audio graph.",
  "main": "dist/main/index.cjs",
  "scripts": {
    "build:code": "node ../../scripts/build.mjs",
    "dev": "npm run build:code && electron .",
    "build": "npm run build:code && electron-builder --win --publish never",
    "release": "npm run build:code && electron-builder --win --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "music-metadata": "^10.6.0",
    "signalsmith-stretch": "1.3.2"
  },
  "devDependencies": {
    "electron": "44.4.0",
    "electron-builder": "^25.1.8"
  }
}
